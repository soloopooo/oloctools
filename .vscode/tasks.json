{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "py打包main",
            "type": "shell",
            "command": "pyinstaller main.py --onefile --out=pyinstaller/out --dist=pyinstaller --icon=icon.ico",
            "problemMatcher": []
        }
    ]
}